# Guild Assets

Place your guild images and videos in this directory.
You can then import them in `src/data.ts` or reference them directly if using the `public` folder.

### Recommended Structure:
- `hero.jpg`: Main banner image
- `characters/`: Folder for member portraits
- `gallery/`: Folder for guild memories
